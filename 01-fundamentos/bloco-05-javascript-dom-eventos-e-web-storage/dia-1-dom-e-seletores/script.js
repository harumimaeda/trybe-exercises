// Título
document.querySelector("#header-container").style.color = "#000";

// 
console.log(document.getElementsByTagName("h3"));

for (let i = 0; i < h3.length; i += 1) {
    h3.style.color = "red";
}


//   let newArray = [];
  if (arrayTech.length === 0) {
    return 'vazio!';
  }
  { for (let i = 0; i < arrayTech.length; i += 1) {
    let newArray = {
      tech: techList[i],
      nome: name,
    };
  }
  }
  newArray.push(arrayTech);
  return techList;
}